{
  "name": "focus-champion",
  "version": "2.0.0",
  "description": "Focus Champion - Gamified Pomodoro Timer",
  "scripts": {
    "build": "mkdir -p www && cp index.html manifest.json sw.js icon-192.png icon-512.png www/ 2>/dev/null || cp index.html manifest.json sw.js *.png www/"
  },
  "dependencies": {
    "@capacitor/android": "5.7.0",
    "@capacitor/core": "5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "5.7.0"
  }
}
